jwt:
  secret: 518725T33i5673y*SojmeTestSecret-+
  token.expiration.seconds: 3600  # 1 час
  cookie.expiration.seconds: 3600


spring:
  output:
    ansi:
      enabled: ALWAYS

  datasource:
    url: jdbc:tc:postgresql:16://localhost/bank_analyzer_test
    username: test
    password: test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    hikari:
      max-lifetime: 10000

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  liquibase:
    change-log: classpath:/db/changelog/db.changelog-test-master.yaml
    contexts: test